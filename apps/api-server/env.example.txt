# ============================================================================
# VARIABLES D'ENVIRONNEMENT - ACADEMIA HUB API
# ============================================================================
# Copier ce fichier vers .env et remplir les valeurs

# Database URLs
# ============================================================================

# URL principale (rôle academia_app - API Backend)
DATABASE_URL="postgresql://academia_app:password@db.xxx.supabase.co:5432/postgres"

# URL admin (rôle academia_admin - Migrations)
DATABASE_ADMIN_URL="postgresql://academia_admin:password@db.xxx.supabase.co:5432/postgres"

# URL ORION (rôle academia_orion - IA analytique, READ ONLY)
DATABASE_ORION_URL="postgresql://academia_orion:password@db.xxx.supabase.co:5432/postgres"

# Supabase (optionnel - si utilisation des features Supabase)
SUPABASE_URL="https://xxx.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Application
# ============================================================================
NODE_ENV="development"
PORT=3000
API_URL="http://localhost:3000"

# JWT
# ============================================================================
JWT_SECRET="CHANGE_ME_IN_PRODUCTION"
JWT_EXPIRES_IN="7d"

# Backup
# ============================================================================
BACKUP_STORAGE_PATH="./backups"

# ORION (IA analytique)
# ============================================================================
ORION_ENABLED=true
ORION_READ_ONLY=true

# Payment Service Provider (Fedapay)
# ============================================================================
FEDAPAY_API_KEY="your-fedapay-api-key"
FEDAPAY_SECRET_KEY="your-fedapay-secret-key"
FEDAPAY_MODE="sandbox" # sandbox | live

# Email (optionnel)
# ============================================================================
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="noreply@academiahub.com"
SMTP_PASSWORD="password"
SMTP_FROM="Academia Hub <noreply@academiahub.com>"

# File Storage (optionnel)
# ============================================================================
STORAGE_TYPE="local" # local | s3
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION=""
AWS_S3_BUCKET=""

# Logging
# ============================================================================
LOG_LEVEL="info" # debug | info | warn | error

